events {
    worker_connections 4096;
}

stream {
    upstream brokers {
        server agent1:9092;
        server agent2:9092;
    }

    server {
        listen 9092;
        proxy_pass brokers;
    }
}
